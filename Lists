//We mostly use the map function

function App() {
  const names = ["Akshat", "Tushar", "Dhruv", "Prakhar"]
  return (
    <div className='app'>
      {names.map((name, key) => {
        <h1 key={key}> {name} </h1>
      })}
    </div>
  )
}

export default App;
